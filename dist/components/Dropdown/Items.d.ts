import React, { PropsWithChildren } from "react";
import { componentBasics } from "../../types/propTypes";
declare const Items: ({ children, className, }: PropsWithChildren<componentBasics>) => React.JSX.Element | null;
export default Items;
